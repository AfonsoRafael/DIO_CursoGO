/*
		Palavras reservadas em Go: 25 no total
		Estas palavras não podem ser usadas como identificadores (nomes de variáveis, funções, etc.)
		break 		case		chan		const
		continue		default		defer		else
		fallthrough		for		func		go
		goto		if		import		interface
		map		package		range		return
		select		struct		switch		type
		var		
		
		bool		byte		error
		float32		float64		int		int8
		int16		int32		int64		rune
		string		uint		uint8		uint16
		uint32		uint64


				Expressões e comandos básicos em Go: por enquanto apenas listados
		Atribuição:         =      (ex: x = 10)
		Atribuição composta: +=, -=, *=, /=, %=, &=, |=, ^=, <<=, >>= (ex: x += 5)
		Operadores aritméticos: +, -, *, /, % (ex: x + y)
		Operadores de comparação: ==, !=, <, <=, >, >= (ex: x == y)
		Operadores lógicos: && (AND), || (OR), ! (NOT) (ex: x && y)
		Operadores de incremento/decremento: ++, -- (ex: x++)
		Operador de endereço: & (ex: &x)
		Operador de desreferência: * (ex: *ptr)
		Operador de tipo: type (ex: var x int = 10; fmt.Printf("%T", x) // imprime "int")
		Comandos fmt.Printf para formatação de saída
		Comandos fmt.Scanln para leitura de entrada
		Comandos básicos de controle de fluxo: if, else, switch, for, range
		Comandos de declaração de variáveis: var, const, := (declaração curta)
		Comandos de retorno de funções: return
		Comandos de controle de loop: break, continue
		Comandos de repetição: for
		Comandos de declaração de funções: func
		Comandos de declaração de pacotes: package, import
		Comandos de manipulação de arrays, slices, maps e structs
		Comandos de manipulação de goroutines e canais para concorrência
		Comandos de manipulação de erros: panic, recover
		Comandos de manipulação de arquivos e I/O: os.Open, os.Create, bufio.NewReader, bufio.NewWriter
		Comandos de manipulação de strings: strings.Split, strings.Join, strings.Replace
		Comandos de manipulação de tempo: time.Now, time.Sleep
		Comandos de manipulação de pacotes: go get, go build, go run, go test
		Comandos de documentação: godoc
		Comandos de depuração: go tool pprof, go tool trace
		Comandos de formatação de código: go fmt
		Comandos de gerenciamento de dependências: go mod init, go mod tidy
		Comandos de testes unitários: go test
		Comandos de profiling: go tool pprof

*/

